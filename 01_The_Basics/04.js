// Explain why this code logs '510' instead of 15.
// console.log('5' + 10);

// Every '+' expression that has a string operand produces a string result.
// Because one operand is a string, JS coerces the other operand into a string,
// then concatenates the two now strings
